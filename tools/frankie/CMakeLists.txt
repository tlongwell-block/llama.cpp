find_package(ICU REQUIRED COMPONENTS uc)
add_library(frankie-text STATIC text-normalization.cpp)
target_link_libraries(frankie-text PUBLIC ICU::uc)
target_compile_features(frankie-text PRIVATE cxx_std_17)
if (TARGET test-mtmd-impl)
    target_link_libraries(test-mtmd-impl PRIVATE frankie-text)
    target_compile_definitions(test-mtmd-impl PRIVATE LLAMA_TEST_FRANKIE)
endif()

set(TARGET llama-frankie-realtime)
add_executable(${TARGET} realtime-server.cpp brain-session.cpp mouth-session.cpp)
target_link_libraries(${TARGET} PRIVATE llama-common mtmd Threads::Threads frankie-text)
target_include_directories(${TARGET} PRIVATE ${PROJECT_SOURCE_DIR}/src)
target_include_directories(${TARGET} SYSTEM PRIVATE ${PROJECT_SOURCE_DIR}/vendor)
target_compile_features(${TARGET} PRIVATE cxx_std_17)
